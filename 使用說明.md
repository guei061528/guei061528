# JIRA Degrade 機制使用說明

## 功能說明

這個系統會自動追蹤測試結果歷史，並智能判斷 degrade（降級）情況。

### Degrade 規則

根據您的需求實作以下規則：

1. **前一版 PASS，下一版 FAIL** → 這就是 **degrade**
   - 建立新的 JIRA issue
   - 標題加上 `[DEGRADE]` 標記
   - 加上 `degrade` label

2. **下一版仍然 FAIL**：
   - **fail 原因一樣** → 在之前的 degrade JIRA 加 comment，說明哪一版仍有問題並附上 log
   - **fail 原因不一樣** → 先判定為 degrade，建立新的 JIRA issue（有 `[DEGRADE]` 標記和 `degrade` label）

## 使用範例

### 場景 1: 第一次失敗（Degrade）

**Version 807**: 測試 PASS  
**Version 808**: 測試 FAIL

```bash
python jira_create.py --project MA --parent MA-1234 --json examples/test_fail_v808.json
```

**結果**:
- 建立 JIRA: `[DEGRADE][MacArthur7P][DB2188][DEMETER][808] test_ccc_11_21_str_dtv - [scaler] MISSING...`
- Labels: `AT`, `Patron`, `STR`, `degrade`

### 場景 2: 相同錯誤持續發生

**Version 808**: 測試 FAIL（錯誤 A）→ 建立 JIRA MA-12345  
**Version 809**: 測試 FAIL（錯誤 A，相同）

```bash
python jira_create.py --project MA --parent MA-1234 --json examples/test_fail_v809_same.json
```

**結果**:
- **不會**建立新的 JIRA
- 在 MA-12345 加上 comment：

```
*Still failing in version 809 (Daily Build: DB2189)*

Test Information
* Image Version: 809
* Daily Build Number: DB2189
* Jenkins URL: http://...
* Log Path: /SRE_AT/...

Fail Log
[scaler] MISSING 'RTK Disable Main ForceBG': Failed to disable Main ForceBG.
```

### 場景 3: 不同錯誤持續發生

**Version 809**: 測試 FAIL（錯誤 A）  
**Version 810**: 測試 FAIL（錯誤 B，不同）

```bash
python jira_create.py --project MA --parent MA-1234 --json examples/test_fail_v810_different.json
```

**結果**:
- 建立新的 JIRA（仍判定為 degrade）
- 標題: `[DEGRADE][MacArthur7P][DB2190][DEMETER][810] test_ccc_11_21_str_dtv - [video] Timeout...`
- Labels: `AT`, `Patron`, `STR`, `degrade`

## 測試資料格式

您的 JIRA 資訊如下（範例）:

```json
{
  "TestInformation": {
    "chipset": "MacArthur7P",
    "Model": "DEMETER",
    "TestcaseName": "test_ccc_11_21_str_dtv",
    "ImageVersion": "808",
    "DailyBuildNumber": "DB2188",
    ...
  },
  "FailLog": {
    "Value": "Failed",
    "Detail": "[scaler] MISSING 'RTK Disable Main ForceBG': Failed to disable Main ForceBG."
  },
  ...
}
```

## 歷史追蹤

系統會在 `~/.jira_test_history.json` 記錄測試歷史：

```json
{
  "MacArthur7P_DEMETER_test_ccc_11_21_str_dtv": {
    "last_result": "FAIL",
    "last_version": "808",
    "last_db": "DB2188",
    "last_fail_hash": "abc123...",
    "last_jira_key": "MA-12345"
  }
}
```

## 錯誤比對邏輯

系統使用 `FailLog.Detail` 欄位來判斷錯誤是否相同：

1. 將錯誤訊息正規化（統一空白、轉小寫）
2. 計算 MD5 hash
3. 比對 hash 值決定是否為相同錯誤

**範例**:
- `"Error   with   spaces"` 和 `"Error with spaces"` → 相同
- `"ERROR MESSAGE"` 和 `"error message"` → 相同
- `"Error A"` 和 `"Error B"` → 不同

## 測試執行

```bash
# 執行所有測試
python -m unittest test_degrade.py -v

# 執行 demo
python demo_degrade.py
```

## 主要檔案

- `jira_create.py` - 主要 JIRA 建立腳本（已整合 degrade 機制）
- `degrade_tracker.py` - Degrade 偵測模組
- `test_degrade.py` - 測試套件（11 個測試案例）
- `demo_degrade.py` - 互動式展示腳本
- `examples/` - 範例測試資料

## 優點

1. ✅ **自動偵測 degrade** - 不需人工判斷
2. ✅ **避免重複 JIRA** - 相同錯誤不會建立重複的 issue
3. ✅ **清楚追蹤** - 所有相關錯誤透過 comment 連結
4. ✅ **智能判斷** - 自動區分相同/不同錯誤
5. ✅ **標記清楚** - `[DEGRADE]` 標題和 `degrade` label

## 建議使用方式

1. **第一次使用**：執行 `python demo_degrade.py` 了解運作方式
2. **整合現有系統**：將 `jira_create.py` 替換您現有的 JIRA 建立腳本
3. **驗證結果**：檢查 `~/.jira_test_history.json` 確認歷史記錄
4. **持續使用**：系統會自動維護歷史並偵測 degrade

## 注意事項

- 歷史檔案位於 `~/.jira_test_history.json`，請勿刪除
- 首次執行時沒有歷史，不會判定為 degrade
- 測試識別基於: `{IC_NAME}_{MODEL}_{TEST_CASE_NAME}`
- 錯誤比對基於: `FailLog.Detail` 欄位的正規化 hash

## 問題排除

**Q: 相同錯誤但建立了新的 JIRA？**  
A: 檢查 `FailLog.Detail` 內容是否完全一致（忽略大小寫和空白）

**Q: 歷史檔案遺失了？**  
A: 系統會自動建立新的歷史檔案，但之前的資料會遺失

**Q: 想要重置歷史？**  
A: 刪除 `~/.jira_test_history.json` 檔案即可

---

如有問題，請參考 `DEGRADE_README.md`（英文）或 `IMPLEMENTATION_SUMMARY.md` 了解更多細節。
